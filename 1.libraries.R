# Widespreadness
# Sam Wolf
# Libraries

# Libraries -------------------------------------------------------------------------------------------
if(!require(mosaic)) {
  install.packages("mosaic");
  require(mosaic)}

if(!require(lattice)) {
  install.packages("lattice");
  require(lattice)}

if(!require(ggplot2)) {
  install.packages("ggplot2");
  require(ggplot2)}

